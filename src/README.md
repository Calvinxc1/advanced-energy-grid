# Advanced Energy Grid

Source folder for the Factorio mod `advanced-energy-grid`.

The mod entrypoint is `info.json`; lifecycle files load electric grid prototypes through `data.lua` and `data-updates.lua`.
